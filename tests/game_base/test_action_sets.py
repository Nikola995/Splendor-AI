from game_base.action_sets import StandardActionSet
from game_base.tokens import Token
from itertools import combinations


def test_standard_action_set_initialization() -> None:
    # action_set = StandardActionSet()
    # assert list(action_set.token_combos) == list(combinations([Token.GREEN,
    #                                                            Token.WHITE,
    #                                                            Token.BLUE,
    #                                                            Token.BLACK,
    #                                                            Token.RED], 3))
    assert True
